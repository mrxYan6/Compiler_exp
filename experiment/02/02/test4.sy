
int fib(int n) {
    if (n <= 0) return 0;
    if (n == 1) return 1;
    return fib(n - 1) + fib(n - 2);
}

void solve() {
    int x;
    x = getint();

    int res = fib(x);

    putint(res);
    putch(10);
}

int main() {
    int id = 21052021;
    putint(id);
    putch(10);

    int t;
    t = getint();
    while (t) {
        solve();
        t = t - 1;
    }

    return 0;
}